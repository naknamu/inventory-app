<!DOCTYPE html>
<html>
  <head>
    <%- include('partials/head'); %>
  </head>
  <body>
    <%- include('partials/nav'); %>

    <h1>Home</h1>
    <p>Welcome to <em>Musical Instrument Inventory App</em>, a very basic Express website.</p>

    <h1><%= title %></h1>

    <%# Check for error %>
    <% if (error) { %>
      <p>Error getting dynamic content.</p>
    <% } else { %>
      <p>The inventory has the following record counts:</p>
    <% } %>

    <ul>
      <li><strong>Categories: </strong><%= data.category_count %></li>
      <li><strong>Instruments: </strong><%= data.item_count %></li>
    </ul>
  </body>
</html>
